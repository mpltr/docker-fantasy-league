
# Install
```bash
    docker-compose up
    docker-compose exec api bash
```
```bash
    composer install
    php artisan migrate 
```
Create .env file in client using the .env.example as template


